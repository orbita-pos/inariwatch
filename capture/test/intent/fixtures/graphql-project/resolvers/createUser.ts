// Resolver fixture — the handler name is the symbol passed to the
// intent compiler. The companion schema lives at ../schema/schema.graphql.

export async function createUser(_parent: unknown, args: unknown) {
  void _parent
  void args
  throw new Error("not implemented")
}
