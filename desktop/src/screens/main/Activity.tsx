export function MainActivity() {
  return (
    <section data-testid="main-activity" className="p-8 max-w-3xl">
      <h1 className="font-[var(--font-serif)] text-2xl mb-2">Activity</h1>
      <p className="text-sm text-[var(--muted)]">
        Last 24 hours of FsChange / ShellEvent / Replay events. Sesión 19 wires
        the real timeline; this is a placeholder anchor.
      </p>
    </section>
  );
}
