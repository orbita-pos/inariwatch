export function MainMemory() {
  return (
    <section data-testid="main-memory" className="p-8 max-w-3xl">
      <h1 className="font-[var(--font-serif)] text-2xl mb-2">Memory</h1>
      <p className="text-sm text-[var(--muted)]">
        <code>memory.md</code> preview + audit history. The IPC surface
        (Sesión 11) is wired; the renderer lands when the per-repo selector
        ships in Sesión 19.
      </p>
    </section>
  );
}
