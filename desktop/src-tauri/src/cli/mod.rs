//! `inari run <cmd>` subcommand + the spawn-with-imports primitive
//! shared by the substrate sensor (Session 10) and Session A's CLI.
//!
//! Session 4 lands [`run`] (RENAMED + EXTRACTED from `src/connect.rs`).
//! Session A formalizes the standalone CLI binary on top.

pub mod run;
