{
  "name": "@inariwatch/ai-router",
  "version": "0.0.1",
  "private": true,
  "description": "InariWatch AI dispatcher — single point of entry for every AI call across web, desktop, CLI, capture, MCP. Per INARI_AI_ARCHITECTURE.md (LOCKED 2026-05-02).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./eslint": "./src/lockdown/eslint-rule.js"
  },
  "scripts": {
    "test": "vitest run --config ./vitest.config.ts",
    "test:watch": "vitest --config ./vitest.config.ts"
  },
  "devDependencies": {
    "eslint": "^10.3.0",
    "vitest": "^4.1.5"
  }
}
